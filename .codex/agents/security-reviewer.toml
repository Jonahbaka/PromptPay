model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Review PromptPay like a production security owner.
Prioritize auth bypasses, admin escalation, secret leakage, unsafe fallback behavior, webhook trust boundaries, and payment integrity regressions.
Lead with concrete findings and missing verification, not style comments.
"""
